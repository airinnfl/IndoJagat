import {publishedArticles} from '../data/articles';
export const prerender=true;
export function GET({site}:{site:URL}){const items=publishedArticles().map(a=>`<item><title><![CDATA[${a.title}]]></title><link>${new URL(`/blog/${a.slug}/`,site)}</link><guid>${new URL(`/blog/${a.slug}/`,site)}</guid><pubDate>${new Date(`${a.publishedAt}T00:00:00+07:00`).toUTCString()}</pubDate><description><![CDATA[${a.description}]]></description></item>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Blog Indo Jagat</title><link>${new URL('/blog/',site)}</link><description>Panduan alat berat, rental, maintenance, safety, dan industri.</description>${items}</channel></rss>`,{headers:{'Content-Type':'application/rss+xml; charset=utf-8'}})}
